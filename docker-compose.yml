services:
  musicmixer:
    build: .
    image: musicmixer:latest
    ports:
      - "8880:8880"
    restart: unless-stopped
    mem_limit: 6g
    env_file: .env
    volumes:
      - musicmixer-data:/app/data

volumes:
  musicmixer-data:
